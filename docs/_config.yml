title: Pipeline to GitHub Actions Converter
tagline: Migrate GitLab, Azure DevOps, and Jenkins pipelines to GitHub Actions — with AI.
description: An open-source .NET 10 tool that uses the GitHub Copilot SDK to intelligently convert GitLab CI, Azure DevOps, and Jenkins pipelines into production-ready GitHub Actions workflows.
url: https://frank802.github.io
baseurl: /ghcp-action-importer
repo: Frank802/ghcp-action-importer
author: Frank802

plugins:
  - jekyll-seo-tag

exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - vendor
